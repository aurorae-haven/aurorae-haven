/**
 * Build directory constants
 * Centralized constants for build output directories to maintain consistency
 * across build scripts and configuration files
 */

// Primary build output directory (used by Vite)
export const DIST_DIR = 'dist'

// Temporary directory for offline build (before archiving)
export const DIST_OFFLINE_DIR = 'dist-offline-build'

// Output directory for offline package archives
export const OUTPUT_DIR = 'dist-offline'
