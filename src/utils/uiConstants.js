/**
 * UI Constants
 * Centralized constants for UI timing, delays, and display settings
 */

// Toast notification display duration (in milliseconds)
export const TOAST_DISPLAY_DURATION_MS = 3800

// Page reload delay after import/export (in milliseconds)
export const PAGE_RELOAD_DELAY_MS = 1500

// Safety window for suppressing navigation prompt (in milliseconds)
export const NAV_PROMPT_SUPPRESS_WINDOW_MS = 2000

// Maximum length for filename sanitization (in characters)
export const FILENAME_MAX_LENGTH = 30

// Default number of backups to keep
export const DEFAULT_BACKUP_LIMIT = 10
